# Copia este archivo a `.env` o exporta estas variables antes de levantar docker-compose

# =====================
# Autenticacion / JWT
# =====================
# Usa un valor largo y aleatorio. NO publiques claves reales en el repo.
AUTH_SECRET=change_me_with_a_long_random_value

# =====================
# IA (opcional)
# =====================
# Requerida solo si usas Genkit + GoogleAI
GEMINI_API_KEY=YOUR_GEMINI_API_KEY

# =====================
# Modo demo
# =====================
# Fuerza el modo NO demo (0/false). Con 1/true ignora Mongo y usa usuario demo.
DEMO_AUTH=0
# Opcional: credenciales del usuario demo si habilitas DEMO_AUTH
DEMO_USER_EMAIL=
DEMO_USER_PASSWORD=

# =====================
# MongoDB
# =====================
# Elige UNA de las opciones siguientes:

# Opcion A) Atlas (recomendada para produccion)
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/?retryWrites=true&w=majority
MONGODB_DB=tutorcritico

# Opcion B) Docker Compose local (el servicio se llama `mongo`)
# MONGODB_URI=mongodb://mongo:27017
# MONGODB_DB=tutor

