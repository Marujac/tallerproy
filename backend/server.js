// Next.js maneja el servidor HTTP. Este archivo sirve como punto de entrada
// documental para la capa backend (controladores, rutas, config, middleware).
// Las rutas reales viven en `src/app/api/*` y delegan en `backend/routes/*`.
export function serverEntry() {
  return 'Backend orchestrated by Next.js API routes';
}
